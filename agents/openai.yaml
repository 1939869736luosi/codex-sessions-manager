interface:
  display_name: "Codex Sessions"
  short_description: "管理和清理本地 Codex 会话记录"
  default_prompt: "Inspect, export, verify, or delete local Codex sessions stored in ~/.codex."

policy:
  allow_implicit_invocation: true
